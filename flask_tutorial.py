'''
More than one @app.route("/") can be used for the same function

Type of converters accepted in paths:
string
int
floate
path (just like strings, but takes "/")
uuid (UUID strings)

There are a lot of Jumanji filters, that we can use
with the variables, inside the template:
    {{ template_variable | title }}
    # This will make every word capitalized
Filters: https://jinja.palletsprojects.com/en/2.11.x/templates/#builtin-filters




'''